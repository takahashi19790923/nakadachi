/**
 * 退会に関する定数。
 *
 * ★依存を持たない専用ファイルにしている。★ 退会画面（クライアント側）が
 * この値を表示に使うため、リポジトリ層に置くとブラウザ側バンドルへ
 * DB ドライバごと引き込まれ、ビルドが落ちる。
 */

/**
 * 退会の申し込みから実際に削除するまでの日数。
 *
 * ★依頼を積むだけの実装にしないこと。★ この日数を過ぎた行を実際に消す
 * 定期処理（scripts/cron.ts の purge-accounts）まで作って初めて
 * 「30日後に削除する」と言える。
 */
export const DELETION_GRACE_DAYS = 30;
