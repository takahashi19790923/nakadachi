# pnpm の既定（シンボリックリンク）は OneDrive 配下 + Windows + workerd の
# 組み合わせで壊れることがある。node_modules を npm と同じ実体配置にして、
# ビルドツールが依存を辿れなくなる事故を避ける。
node-linker=hoisted

# 依存はすべて完全一致で固定する（package.json にキャレットを書かない方針と対）。
save-exact=true
