# 脅威モデル

OWASP ASVS と OWASP Top 10 を下敷きに、このサービス固有の事情を足しています。

---

## 1. 守るもの

| # | 資産 | 失われるとどうなるか |
|---|---|---|
| A1 | 利用者のメールアドレス | 名簿として売買される。フィッシングの起点になる |
| A2 | 利用者の居場所につながる情報 | 対面取引が前提のため、住居が特定される危険が直接ある |
| A3 | やり取りの内容 | 取引の交渉、時に個人的な事情が含まれる |
| A4 | 掲載料の決済 | 金銭の損失。決済事業者からの信用の喪失 |
| A5 | 掲載枠の正当性 | 無料で公開されると事業が成立しない |
| A6 | 管理者権限 | 全データの閲覧・改ざん・削除 |
| A7 | サービスの継続 | 止まると取引が止まる |

---

## 2. 想定する攻撃者

| # | 誰 | 動機 | 持っているもの |
|---|---|---|---|
| T1 | 通りすがりの攻撃者 | 面白半分、自動化された走査 | 公開されている情報のみ |
| T2 | 悪意のある利用者 | 無料掲載、他人へのなりすまし、詐欺 | 正規のアカウント |
| T3 | 詐欺グループ | 金銭。闇バイトの募集 | 複数のアカウント、使い捨てのメールアドレス |
| T4 | ストーカー的な個人 | 特定の人物の居場所を知る | 執着と時間 |
| T5 | 競合・いたずら | サービスを止める | 帯域と自動化 |
| T6 | 内部（管理者アカウントの乗っ取り） | 全データ | Gmail への侵入 |

---

## 3. 脅威と対策

### T1・T2 — 認証まわり

| 脅威 | 対策 | 残るリスク |
|---|---|---|
| 会員名簿の作成（アドレスの存在確認） | 登録の有無で応答も挙動も変えない。未登録でもトークンを作りメールを送る | 送信の有無をメール側から観測されれば分かる（受信箱を持っている場合のみ） |
| 6桁 OTP の総当たり | 試行5回でトークンごと無効。IP 単位・トークン単位のレート制限。有効期限15分 | 大量の IP を持つ攻撃者は試行回数を稼げる |
| ログインリンクの盗み見 | 一度使えば無効。15分で失効 | メールボックスを奪われていれば防げない |
| セッションの乗っ取り | Cookie は `__Host-` / HttpOnly / Secure / SameSite=Lax。DB にはハッシュのみ | 端末を物理的に奪われた場合 |
| セッション固定 | ログインのたびに新しいセッションを作る | — |
| 認証情報の使い回し | **パスワードが存在しない** | — |

### T2 — 掲載料の回避

| 脅威 | 対策 |
|---|---|
| 決済せずに公開する | 公開できるのは**署名検証済み Webhook を受けた経路だけ**。success URL では状態を変えない |
| 偽の Webhook を送る | 署名検証（HMAC-SHA256、時刻の許容差5分）。単体テストで9通りの改ざんを確認 |
| 金額を書き換える | 金額はサーバー側の定数。クライアントから受け取らない。Webhook 側でも 110円/jpy を照合 |
| 他人の決済を自分の投稿に流用する | metadata の `listing_id` / `user_id` を決済記録と突き合わせる |
| 同じ決済を二度使う | `checkout_session_id` と `event_id` に一意制約 |
| 掲載終了後に再公開する | `closed → published` の遷移が存在しない |
| 返金してから使い続ける | `charge.refunded` / `refund.*` / `dispute` を購読し、全額返金で自動的に非公開 |

### T3 — 詐欺・違法な募集

| 脅威 | 対策 | 残るリスク |
|---|---|---|
| 闇バイトの募集 | 禁止ワード（遮断／要確認）、通報、管理者による非公開 | **表記ゆれで簡単にすり抜ける。機械的な遮断だけでは足りない** |
| 口座・アカウントの売買 | 同上。禁止出品物のページに明記 | 同上 |
| 前払い詐欺 | 安全ガイドで注意喚起。サービスは代金を預からない | **当事者間の取引には介入できない** |
| 使い捨てアドレスでの大量登録 | Turnstile、レート制限 | 使い捨てアドレスの遮断は行っていない |
| 同一内容の連投 | 投稿のレート制限（20件/時）、掲載ごとに110円 | 費用を払えば可能 |

> **ここは技術だけでは守れません。** 通報の運用と、管理者の目視が前提です。
> 通報件数の監視を怠らないでください。

### T4 — 居場所の特定

| 脅威 | 対策 | 残るリスク |
|---|---|---|
| 投稿に書いた住所から特定される | 地域は市区町村まで。番地らしき並びを検証で拒否。入力欄と安全ガイドで注意 | 「〇〇公園の向かい」のような書き方は防げない |
| 写真の Exif から特定される | **JPEG の APPn・PNG の eXIf/tEXt・WebP の EXIF を除去**。単体テストで確認 | 写っているもの（表札・景色）からの特定は防げない |
| プロフィールからの推測 | 表示名は自由。本名を求めない | — |
| メッセージから聞き出される | 安全ガイドで注意喚起。ブロック機能 | **相手が自分で答えてしまえば防げない** |
| 待ち合わせ場所での接触 | 安全ガイド（人目のある場所・明るい時間帯・自宅を指定しない） | 対面取引そのものが持つリスク |

### T1・T5 — 投げ込み系

| 脅威 | 対策 | 残るリスク |
|---|---|---|
| CSRF | Origin/Referer の完全一致 ＋ 署名付き二重送信トークン。両方必須 | — |
| XSS | React の既定エスケープ。CSP は nonce 方式で `'unsafe-inline'` 無し | CSP を緩めた瞬間に効かなくなる（テストで検出） |
| クリックジャッキング | `X-Frame-Options: DENY` ＋ `frame-ancestors 'none'` | — |
| SQL インジェクション | プレースホルダ。生 SQL もパラメータ化。LIKE のメタ文字をエスケープ | — |
| SVG による XSS | **拡張子ではなく先頭バイトで判定し、SVG を拒否** | — |
| 画像による DoS | 5MB / 6000px / 10枚の上限 | 上限内での大量投稿は費用（110円）が抑止 |
| オープンリダイレクト | 戻り先はパスのみ。`//` とバックスラッシュも拒否 | — |
| SSRF | 外部への接続先は定数 URL のみ | — |
| 総当たりでの ID 探索 | ULID（暗号論的乱数）＋ 全 API での権限確認 ＋ **404 で返す** | — |
| 大量アクセス | アプリ側のレート制限。Cloudflare の既定の保護 | **エッジの Rate limiting rules は Free だとゾーンで1枠しか無く、当てにできない** |

### T6 — 管理者権限

このアカウントは**全サービスで同一のメールアドレス・同一のパスワード**を
使っています。**Gmail が陥落したときに残るのは第3層だけ**です。

| 層 | 内容 | 破られる条件 |
|---|---|---|
| 1 | メールでのログイン | Gmail への侵入 |
| 2 | 管理者だけの再認証（別のメール確認コード） | 同上 |
| 3 | 全プロジェクト共通の追加資格情報 | Vault への侵入 |

- 管理者は `運営者本人のアドレス` のみ。他のアドレスに権限を与えない
- 第3層の通過証は12時間で失効
- **通過証の署名鍵は資格情報そのものから作っている**ため、値を変えれば
  発行済みの通過証がその場で全部無効になる
- 管理操作はすべて `admin_actions` と `audit_logs` に記録し、理由を必須にする
- 通報対応で他人の会話を読む場合も記録が残る

> **ブラウザの Basic 認証ダイアログを使っていません。** fetch 主体の画面では
> 401 に対して資格情報の窓が出ず、正しい値を知っていても入れなくなります。
> **curl では通るため、実装者は「動いた」と誤認します。** 別サービスで実際に
> 起きた事故です。守る強さは変えず、画面内の入力欄で受けています。

### Turnstile の共有ウィジェット

このアカウントは Free プランの上限（20個）に達したため、
**1つのウィジェットを全サービスで共有**しています。

そのため、あるサービス向けに解かれたトークンが、そのまま別のサービスでも
siteverify を通ります。siteverify は `success: true` としか言いません。

**サービスを分けられるのはサーバー側の hostname 照合だけです。**

- `TURNSTILE_EXPECTED_HOSTS` と**完全一致**で比べる（部分一致だと
  `nakadachi.rewrite-co.com.evil.example` が通る）
- **未設定なら拒否する（fail-close）**
- `.pages.dev` を書かない（誰でも `*.pages.dev` を取得できるため）

---

## 4. 残余リスク（受け入れているもの）

| # | リスク | なぜ受け入れるか | 見直す条件 |
|---|---|---|---|
| R1 | 当事者間の取引トラブル | サービスは場を提供するのみ。介入すると責任の所在が変わる | — |
| R2 | 禁止ワードのすり抜け | 日本語の表記ゆれを機械的に潰しきれない | 通報件数が跳ねたら辞書と運用を見直す |
| R3 | 写真に写り込んだ情報からの特定 | 画像認識は MVP の範囲外 | 実際に被害の報告があれば検討 |
| R4 | 使い捨てアドレスでの登録 | 正当な利用者を弾く副作用が大きい | 乱用が目立つようになったら |
| R5 | 深いページの検索性能 | 200ページで打ち切っている | 件数が数千件を超えたら外部検索へ |
| R6 | 2文字以下の検索語 | pg_trgm の仕様 | 同上 |
| R7 | Cloudflare / Neon / Stripe の障害 | 単一の事業者に依存している | 冗長化は費用に見合わない |
| R8 | メール送信の失敗 | 決済済み投稿の公開はロールバックしない設計 | 記録が残るので再送で回復できる |
| R9 | 管理者アカウントが1つ | 運用者が1人のため | 運用者が増えたら |

---

## 5. 検出できるようにしていること

| 何を | どこで |
|---|---|
| Webhook の処理失敗 | `payment_webhook_events.status = 'failed'` |
| メール送信の失敗 | `email_delivery_logs.status = 'failed'` |
| 未対応の通報 | 管理ダッシュボードのバッジ |
| 管理操作 | `admin_actions` / `audit_logs` |
| 設定の投入漏れ | `/api/config` の `secretsConfigured` |
| DB の疎通 | `/api/health` |
| ハイドレーションの破壊 | `e2e/hydration.spec.ts`（実ブラウザ） |
| CSP の緩和 | `test/security.test.ts` |
| 退会削除の失敗 | `scripts/cron.ts purge-accounts` が終了コード1で落ちる |

---

## 6. 定期的に見直すこと

- 禁止ワードの辞書（通報の傾向を見ながら）
- 依存の脆弱性（Dependabot と `pnpm audit`）
- 監査ログの内容（個人情報が混ざっていないか）
- レート制限の値（正当な利用者を弾いていないか）
- **この文書自体**（機能を足したら脅威も増える）
